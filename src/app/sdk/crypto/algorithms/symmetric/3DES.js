const crypto = require('crypto');

class TripleDES {
    constructor() {
        const name = "3DES";
        const block_size = 64;
        const key_sizes = [64, 128, 192];
    }

    encrypt(key, iv, data) {
        
    }

    decrypt(key, iv, data) {
        
    }
}